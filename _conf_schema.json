{
  "proxy_base": {
    "description": "反向代理基址(路径式,默认 https://ytproxy.luoh.org)。所有发往 youtube.com / googlevideo.com / ytimg.com / youtu.be 的请求会改写成 <proxy_base>/<原域名>/<原路径>,由 Worker 反代。设为空则禁用反代。",
    "type": "string",
    "default": "https://ytproxy.luoh.org"
  },
  "proxy": {
    "description": "兜底 HTTP/SOCKS 代理(可选),例如 http://127.0.0.1:7890。仅在 proxy_base 为空时生效。",
    "type": "string",
    "default": ""
  },
  "cookies_file": {
    "description": "YouTube cookies 来源(Netscape 格式)。可填:1) 本地路径,如 /AstrBot/data/music.youtube.com_cookies.txt;2) http(s) URL,会自动下载并缓存到临时目录。yt-dlp 用它绕过『Sign in to confirm you're not a bot』。",
    "type": "string",
    "default": "/AstrBot/data/music.youtube.com_cookies.txt"
  },
  "cookies_refresh_seconds": {
    "description": "当 cookies_file 是 URL 时,本地缓存的有效期(秒),默认 3600。超过则重新下载。",
    "type": "int",
    "default": 3600
  },
  "send_card": {
    "description": "是否尝试发送 QQ 音乐卡片(仅 aiocqhttp 平台,部分协议端不支持)",
    "type": "bool",
    "default": true
  },
  "send_audio": {
    "description": "是否下载并发送音频文件(需安装 yt-dlp 与 ffmpeg)",
    "type": "bool",
    "default": true
  },
  "max_duration": {
    "description": "允许下载发送的最大歌曲时长(秒),超过则跳过音频下载",
    "type": "int",
    "default": 600
  }
}
